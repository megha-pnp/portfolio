# Portfolio project

This repository contains a portfolio site that was originally authored with static HTML/CSS/JS. The site has been migrated into a React + Vite app located in the `client/` folder.

To run the React app:

1. cd client
2. npm install
3. npm run dev

The original static admin and dashboard pages remain available under `client/public/`.
